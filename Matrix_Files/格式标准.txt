

# 我感觉直接用csv的格式就不错。逗号点阵，xyz 加一个数字代表粒子颜色。
# 二值
# x,  z,  y,  particle_color (如果是二值，则颜色只有0，1，2三种，分别对应，黑，白，透明)
10, 10, 10, 1

# 对于相对坐标 ~x ~z ~y
# 其中~x 是东+西-，~z是上+下-， ~y是南+北-
# 对于实体视角坐标 ^x ^z ^y
# 其中^x 是左+右-，^z是上+下-，^y则是前+后-。

#　彩色
# x,  z,  y,  particle_color (彩色粒子通过RGB表示）
10, 10, 10, ff, ff, ff

# 生成的一组坐标矩阵应该被放置在相应的文件夹下：
Matrix_Files
    Square_effect
        1.csv 或.txt
        2.csv 或.txt
        3.csv 或.txt
